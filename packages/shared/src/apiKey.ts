/** HTTP header name for passing the API key. */
export const API_KEY_HEADER = "x-api-key";

/** localStorage key for persisting the API key in the browser. */
export const API_KEY_STORAGE_KEY = "pw-api-key";
